// Canonical language registry now lives in @marshrutizator/shared. Re-exported here so
// existing imports of './supportedLanguages' continue to work unchanged.
export { SUPPORTED_LANGUAGES, SUPPORTED_LANGUAGE_CODES } from '@marshrutizator/shared'
export type { SupportedLanguageCode } from '@marshrutizator/shared'
